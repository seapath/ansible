# Copyright (C) 2025, RTE (http://www.rte-france.com)
# SPDX-License-Identifier: Apache-2.0

name: CI

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]
  workflow_dispatch:

permissions:
  checks: write
  contents: read

jobs:
  ci-qa:
    uses: ./.github/workflows/ci-qa.yml

  ci-molecule:
    needs: ci-qa
    uses: ./.github/workflows/ci-molecule.yml

  ci-debian:
    needs: ci-molecule
    uses: ./.github/workflows/ci-debian.yml

  ci-sles:
    needs: ci-molecule
    uses: ./.github/workflows/ci-sles.yml

  ci-oraclelinux:
    needs: ci-molecule
    uses: ./.github/workflows/ci-oraclelinux.yml

  ci-yocto:
    needs: ci-molecule
    uses: ./.github/workflows/ci-yocto.yml

  ci-virtual-cluster:
    needs: ci-molecule
    uses: ./.github/workflows/ci-virtual-cluster.yml
