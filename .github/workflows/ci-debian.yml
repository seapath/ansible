# Copyright (C) 2023, RTE (http://www.rte-france.com)
# SPDX-License-Identifier: Apache-2.0

name: CI debian

env:
  WORK_DIR: /tmp/seapath_ci_${{ github.run_id }}_${{ github.run_attempt }}_${{ github.sha }}

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [debian-main]


jobs:
  CI:
    runs-on: self-hosted
    steps:

      - name: Initialize sources
        run: mkdir ${{ env.WORK_DIR }}; cd ${{ env.WORK_DIR }};
             git clone -q --depth 1 -b latency-tests https://github.com/seapath/ci ci;
             echo "CI sources downloaded successfully";
             ci/launch.sh init;

      - name: Configure Debian
        run: cd ${{ env.WORK_DIR }};
             ci/launch.sh conf;

      - name: Launch test
        run: cd ${{ env.WORK_DIR }};
             ci/launch.sh test;

      - name: Clean
        if: always()
        run: rm -rf $WORK_DIR;
